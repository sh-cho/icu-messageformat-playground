# Cache task outputs across builds (local + CI via gradle/actions/setup-gradle).
org.gradle.caching=true
# Reuse the configuration phase when inputs are unchanged. The version is computed
# via providers.exec (a config-cache-safe API), so this stays compatible.
org.gradle.configuration-cache=true
# Run independent tasks in parallel.
org.gradle.parallel=true
