# Cache task outputs across builds (local + CI via gradle/actions/setup-gradle).
org.gradle.caching=true
# Reuse the configuration phase when inputs are unchanged (version uses the
# config-cache-safe providers.exec API).
org.gradle.configuration-cache=true
# Run independent tasks in parallel.
org.gradle.parallel=true
